/**
 * Componentes compartidos web. Por ahora solo el helper `cn`.
 * Aquí migraremos progresivamente Button, Input, Card, Badge, Table...
 * a medida que aparezcan en más de un sitio.
 */
export { cn } from './cn';
